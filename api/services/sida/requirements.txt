# Pinned to match the official hzlsaber/SIDA requirements.txt exactly
# (minus torch/torchvision, which the Dockerfile installs separately as
# CPU-only wheels — see the comment there for why).
# This wraps real upstream modeling code (LlavaLlamaForCausalLM, SAM,
# deepspeed-based checkpoint format) that is not guaranteed to load correctly
# under newer transformers/peft — do not casually bump these.
packaging
sentencepiece
peft==0.4.0
einops==0.4.1
fastapi==0.100.1
gradio==3.39.0
markdown2==2.4.10
numpy==1.24.2
openai==0.27.8
opencv_python==4.8.0.74
Pillow==9.4.0
pycocotools==2.0.6
ray==2.6.1
Requests==2.31.0
shortuuid==1.0.11
tqdm==4.64.1
transformers==4.31.0
uvicorn==0.23.2
scipy==1.11.2
bitsandbytes==0.41.1
torchviz
tensorboard
deepspeed==0.14.0
python-multipart
